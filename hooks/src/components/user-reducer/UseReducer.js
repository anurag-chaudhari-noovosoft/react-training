import Form from "./Form";
import TodoApp from "./TodoApp";

export default function UseReducer() {
    return (
        <div>
            <p>Use Reducer</p>
            <Form/>
            <TodoApp/>
        </div>
    )
}